# Local development only — copy to .env and fill in. Never commit real tokens.
# GRDM_TOKEN is optional here; the Flet app stores PAT in OS secure storage instead.
GRDM_PROJECT_ID=
GRDM_FOLDER_ID=
# GRDM_TOKEN=
