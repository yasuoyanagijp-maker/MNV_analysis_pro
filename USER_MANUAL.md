# ARIAKE OCTA — ユーザーマニュアル

加齢黄斑変性症（MNV）および血管密度（VD）解析向けのデスクトップ／ブラウザ UI です。本書は**研究・臨床オペレーター**向けの操作説明です。

---

## 1. 概要

| 項目 | 内容 |
|------|------|
| アプリ名 | ARIAKE OCTA（Retinal Analysis Pro 系） |
| 主な用途 | OCTA 等の画像に対する **MNV 解析**、**VD 解析**（単一／バッチ） |
| フロント | Flet（Python） |
| API | FastAPI（既定で `http://127.0.0.1:8000` に接続） |

---

## 2. 動作環境

- **Python** 3.9 以上（プロジェクトでは 3.9+ 想定）
- **OS** 例：macOS（`run_flet.sh` は macOS 向けコメントあり）
- 仮想環境（`.venv`）への依存パッケージインストールが前提です。

---

## 3. セットアップ（初回）

1. リポジトリを取得し、プロジェクトルートに移動します。  
2. 仮想環境を作成・有効化し、依存関係を入れます。

```bash
python3 -m venv .venv
source .venv/bin/activate   # Windows は .venv\Scripts\activate
pip install -r requirements.txt
```

---

## 4. 起動方法

### 4.1 推奨：一体起動スクリプト（バックエンド + UI）

```bash
./run_flet.sh
```

スクリプトは次を行います。

- ポート **8000**（API）と **FLET_PORT**（既定 **8550**、Flet）の空き確認
- **FastAPI** をバックグラウンドで起動（ログは `backend.log`）
- **Flet** フロントを起動

### 4.2 手動起動

ターミナルを二つに分ける例です。

**API：**

```bash
source .venv/bin/activate
python src/api/main.py
```

**UI：**

```bash
source .venv/bin/activate
flet run --web --port 8550 main_app.py
```

または `python main_app.py`（`FLET_USE_WEB` により挙動が変わります。次節）。

### 4.3 ブラウザで開く

`FLET_USE_WEB=1`（既定）のとき、**Web ブラウザ**で UI が開きます。`run_flet.sh` は既定で **`FLET_SERVER_IP=127.0.0.1`** を渡すため、自動で開くタブは **`http://127.0.0.1:<FLET_PORT>`**（既定ポート 8550）になります。手動で開く場合も同じ URL を使ってください。白紙のタブのままになる場合は、ホストに **`0.0.0.0` を使わない**（ループバック推奨）ことと、ファイアウォールを確認してください。

---

## 5. 表示モード：Web とネイティブ

Flet では **ブラウザ（Web）** と **ネイティブウィンドウ** を切り替えられます。環境変数 **`FLET_USE_WEB`** で統一しています。

| 設定 | 意味 |
|------|------|
| `FLET_USE_WEB=1`（省略時と同じ） | **ブラウザ**で表示。OS のフォルダ選択ダイアログ（`get_directory_path`）は**使えません**（Flet の制限）。 |
| `FLET_USE_WEB=0` または `native` 等 | **ネイティブウィンドウ**。OS のファイル／フォルダダイアログが利用可能な場合があります。 |

**例（ネイティブで起動）：**

```bash
FLET_USE_WEB=0 ./run_flet.sh
```

**例（ポート変更）：**

```bash
FLET_PORT=9000 ./run_flet.sh
```

Web モードでは、フォルダ／画像の指定に **手動パス入力**や、**API が参照できるパス**での**サーバー側フォルダ一覧**（アプリ内のエクスプローラ）が補助として使われます。解析対象データは、**API を動かしているマシン上**（またはマウントされたパス）に置く必要があります。

---

## 6. ログイン

- 通常起動時は **`/login`** から開始し、**研究者名**と**パスワード**を入力します。
- **Institution code（施設コード）** をプルダウン（または Custom 手入力）で選びます。学習用エクスポートや多施設集計で表記揺れを防ぐための **コード化された ID** です（環境変数 `ARIAKE_INSTITUTION_ID` があればそちらが優先されます）。
- **Role / 読影担当** をプルダウンで選びます。**第1グレーダー（施設読影）** が既定で、二重読影を行う場合は **第2リーダー（セカンドリーディング）** を選択します（§10.2 参照）。
- ログイン成功後、ホーム（`/`）へ遷移します。研究者名は CSV の Analyst / メタデータの `rater_id` に使われます。
- API の `/login` と連携します。接続できない場合はエラーメッセージを確認してください。

### 開発者向け注意（DEV_MODE）

環境変数 **`DEV_MODE=1`** を付けて起動すると、**ログインがバイパス**され、セッションに開発用ユーザーが入ります。**本番・共有端末では絶対に使わないでください。**

---

## 7. 画面とナビゲーション

ログイン後、左の**サイドバー**から主要画面へ移動できます。

| アイコン（目安） | 遷移先 | 内容 |
|------------------|--------|------|
| ホーム | `/` | **ダッシュボード**（解析種別・パス指定・バッチ） |
| 解析 | `/` または解析フロー | MNV / VD の各ステップ（`/roi`・`/mnv` など） |
| 結果 | `/results` | 解析結果の表示 |

ルートは URL パスと対応しています（例：`/results`）。

---

## 8. ダッシュボード（ホーム）

### 8.1 解析タイプ

- **MNV Analysis** — 単一画像の MNV 解析（ROI 設定へ進む場合があります）。
- **VD Analysis (Single)** — 単一画像向け VD。
- **VD Analysis (Batch Folder)** — フォルダ単位のバッチ。

**Image Scale (mm)** は画像スケール設定として利用します（解析 API に渡されます）。

### 8.2 画像・フォルダの指定

- **Select Folder / Select Image**  
  - **ネイティブ**：OS のダイアログで選べる場合があります。  
  - **Web**：サーバー側パスを選ぶ UI や、**手動パス**が主な手段です。
- **サーバー側のフォルダ一覧（エクスプローラ型）**でファイルを選んだあと、**「Confirm Selection」** ボタンで確定します。**画像のダブルクリックでは進みません**（仕様）。要点だけ知りたい場合は **[documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md](documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md)** を参照してください。
- **Manual Path**  
  ファイルまたはフォルダの**絶対パス**を貼り付けて利用できます。引用符（`'` `"`）は自動で除かれることがあります。
- **ドラッグ＆ドロップ**  
  対応している場合、ドロップしたパスで処理が始まります。

### 8.3 バッチ解析

フォルダ内の対応画像を一覧に載せ、**Start Batch Analysis** でキュー実行します。完了後、**結果画面**へ進む動作になります。

**対応拡張子の例**（ダッシュボードの走査に基づく）：`.tif` `.tiff` `.png` `.jpg` `.jpeg`

---

## 9. ROI 画面（`/roi`）と MNV ウィザード（`/mnv`）

- **MNV** 系のフローでは、画像と解析設定に応じて **ROI（関心領域）** の指定やウィザード画面が表示されます。
- 画面の指示に従い、**戻る／次へ**や**解析開始**を行ってください。
- **Skip — MNV absent**: MNV がはっきり見えない場合に使います。形態計測は実行せず、学習用に **空（全黒）マスク** と **`mnv_present=false`** を残します。CSV では数値は空、`MNV present=0`、`Quality of analysis=N/A`（Fail ではありません。QC と病変有無は別フィールドです）。

---

## 10. 結果画面（`/results`）

- 直近の解析結果やバッチ結果のサマリーを閲覧します。
- **Save CSV** — ImageJ 互換の MNV / VD バッチ CSV を出力フォルダへ保存します。
- **Export Metadata & Data** — **MedSAM 型**データ（`export/images|masks|meta/…` + `manifest.csv`）に加え、個別 PDF を **`export/pdfs/{institution_id}/{lesion_id}.pdf`** へ一括書き出します（バックグラウンド実行）。
- データ量が多いと表示に時間がかかる場合があります。

### 10.1 MNV の主要スコア（Complexity / Uniformity）

結果画面のタイルと CSV 列の対応:

| UI | CSV 列 | 意味 |
|----|--------|------|
| **Complexity** | Network Complexity Score | 血管網の複雑性（0–100） |
| **Uniformity** | Caliber Uniformity Score | 口径の均一性（0–100） |
| Maturity Index | Maturity Index | \(50+(\mathrm{Uniformity}-\mathrm{Complexity})/2\) |

**現行実装は層別 PCA**です（マクロ版の「7 成分／6 成分の単純加重」ではありません）。

- **Complexity**: Euler（符号反転）・ループ数・分岐密度・フラクタル次元を Z 化し、層別主成分＋正規化 Trunk を EVR で合成。  
- **Caliber Uniformity**: 放射状 10 bin の径プロファイルから CV・隣接変化・残差 CV・レンジ％を算出し、PCA（0.7·(−PC1)+0.2·PC2+0.1·Trunk）で合成。  
- 層（`small` / `large` / `small_3mm`）は **Image Scale (mm)** と画像幅で決まります。

パラメータ表・式の詳細は **[documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md](documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md) §6.3** を参照してください。Uniformity が厳密に **25.0** のときは再解析を勧める警告が出ます（計算上の固定センチネルではありません）。

### 10.2 第2リーダー（二重読影・統合解析データ）

第1グレーダーの解析結果に対する **セカンドリーディングとデータ統合** のワークフローです。

**前提:** 第1グレーダーが解析後に **Save CSV** と **Export Metadata & Data** を実行済みであること（出力フォルダに `MNV_*.csv` と `export/images|masks|meta/…` が存在する）。

1. **ログイン** — 施設コードを選択し、Role で **第2リーダー** を指定してログインします。
2. **自動スキャン** — ダッシュボードに表示される「第2リーダー読影を開始（自動スキャン）」から、**メタデータフォルダの親フォルダ**（第1グレーダーの出力フォルダ。`export`／`meta` フォルダ自体を選んでも自動で正規化されます）を選択します。通常のファイル指定は不要で、エキスポート済み画像が自動検出され順次読影キューに入ります。同時に第1グレーダーの CSV も自動検索されます。
3. **読影** — 通常の MNV フローと同じく、画像ごとに ROI 指定 → 解析 → QC（OK / Redo）を行います。
4. **エキスポート** — 最終画面（結果サマリー）で **Save CSV** を押すと、第2リーダーの CSV が `second_reader_output_YYYY_MM_DD/` に保存され、画面に **「統合解析データ」ボタン** が表示されます（エキスポートを実行したときのみ表示されます）。
5. **統合解析データ** — ボタンを押すと次を自動実行します。
   1. 第1グレーダーの CSV を自動検索
   2. 第2リーダーの CSV と行をファイル名で突合
   3. 両CSVで Caliber/Maturity **U2** を再計算のうえ、**RPD（Relative Percent Difference）≤ 20%** なら**算術平均を採用**、超過なら **NA（要再計測）** — 既存の reading-center RPD20 判定コード（`tools/reading_center_rpd/`）を使用
   4. 統合 CSV（`MNV_integrated_*_adopted_values.csv`）、再計測リスト（`*_recheck_list.csv`）、サマリー（`*_summary.md`）を出力

\[
\mathrm{RPD}=\frac{|A-B|}{(|A|+|B|)/2}\times 100
\]

---

## 11. トラブルシューティング

| 現象 | 確認すること |
|------|----------------|
| ブラウザが開かない | `FLET_PORT` が他プロセスと競合していないか、ファイアウォールを確認。 |
| API に接続できない | バックエンドが起動しているか、`127.0.0.1:8000` に届くか。 |
| Web でフォルダが選べない | **仕様上、OS のフォルダダイアログは使えません。** 手動パス、サーバー側エクスプローラ、または **`FLET_USE_WEB=0`** でネイティブ起動を試す。 |
| パスが通らない | パスが **API プロセスから見える**か（共有フォルダのマウント、権限）。 |
| ログインできない | `DEV_MODE` ではないことを確認。API の認証とネットワークを確認。 |

---

## 12. ファイルとログ

| ファイル / 場所 | 用途 |
|-----------------|------|
| `run_flet.sh` | バックエンド + Flet の一括起動 |
| `main_app.py` | Flet エントリ（`FLET_USE_WEB` / `FLET_PORT` / `DEV_MODE`） |
| `src/api/main.py` | FastAPI バックエンド |
| `uploads/` | アップロード用（実行時に作成されることがある） |
| `backend.log` | API 起動をスクリプトでリダイレクトしている場合のログ例 |
| `export/images|masks|meta/{institution_id}/` + `manifest.csv` | Metadata & Data（MedSAM 型・OCTA MNV ROI） |

---

## 13. 注意事項

- 本アプリの利用は、施設の**情報セキュリティ方針**と**臨床研究倫理**に従ってください。
- 解析の医学的解釈は、**専門家の判断**を前提とし、本マニュアルはソフトの操作手順の説明に限ります。

---

## 14. 関連ドキュメント

- **操作の要点（Confirm Selection 等）:** [documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md](documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md)
- **理論・出力定義・Complexity / Caliber Uniformity:** [documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md](documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md)（§6.3）
- **開発者向け（実装・拡張）:** [DEVELOPER.md](DEVELOPER.md)

---

## 15. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-07 | **第2リーダー（二重読影）機能**を追加：ログインの Role 選択、メタデータ親フォルダの自動スキャン、エキスポート後の **統合解析データ** ボタン（RPD≤20% 採用ルールで第1・第2 CSV を統合）。§6・§10.2 を追記。 |
| 2026-08-07 | ログインに **Institution code** を追記。結果画面に **Export Metadata & Data**（`export/{institution_id}/{lesion_id}/`）を追記。§10.1 に **Complexity / Caliber Uniformity**（層別 PCA）の要約と詳細マニュアル §6.3 へのリンクを追加。関連ドキュメント・ログ表を更新。 |
| （既往） | 起動（`run_flet.sh`）、Web／ネイティブ切替、ログイン、ダッシュボード、ROI／結果画面、トラブルシューティングを記載。 |

---

*文書の対象：リポジトリ内の Flet フロントおよび付属起動手順。バックエンド API の詳細は `src/api` および利用環境の仕様に従ってください。*
