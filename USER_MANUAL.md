# ARIAKE OCTA — ユーザーマニュアル

加齢黄斑変性症（MNV）および血管密度（VD）解析向けのデスクトップ／ブラウザ UI です。本書は**研究・臨床オペレーター**向けの操作説明です。

**二重読影・多施設データ共有（GakuNin RDM）** の手順は **§6A** および **§10.2〜§10.4** にまとめています。各サイト／Team YY 向けの協働通知は [collaboration.md](documentation/collaboration.md)、ROI の指定（手動囲み・Erase・血管同定）は [roi_method.md](documentation/roi_method.md)、フォルダ選択の最短手順は [簡易版](documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md)、指標定義は [詳細マニュアル V2](documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md) を参照してください。

---

## 1. 概要

| 項目 | 内容 |
|------|------|
| アプリ名 | ARIAKE OCTA（Retinal Analysis Pro 系） |
| 主な用途 | OCTA 等の画像に対する **MNV 解析**、**VD 解析**（単一／バッチ） |
| フロント | Flet（Python） |
| API | FastAPI（既定で `http://127.0.0.1:8000` に接続） |

---

## 2. 動作環境

- **Python** 3.9 以上（プロジェクトでは 3.9+ 想定）
- **OS** 例：macOS（`run_flet.sh` は macOS 向けコメントあり）
- 仮想環境（`.venv`）への依存パッケージインストールが前提です。

---

## 3. セットアップ（初回）

1. リポジトリを取得し、プロジェクトルートに移動します。  
2. 仮想環境を作成・有効化し、依存関係を入れます。

```bash
python3 -m venv .venv
source .venv/bin/activate   # Windows は .venv\Scripts\activate
pip install -r requirements.txt
```

---

## 4. 起動方法

### 4.1 推奨：一体起動スクリプト（バックエンド + UI）

```bash
./run_flet.sh
```

スクリプトは次を行います。

- ポート **8000**（API）と **FLET_PORT**（既定 **8550**、Flet）の空き確認
- **FastAPI** をバックグラウンドで起動（ログは `backend.log`）
- **Flet** フロントを起動

### 4.2 手動起動

ターミナルを二つに分ける例です。

**API：**

```bash
source .venv/bin/activate
python src/api/main.py
```

**UI：**

```bash
source .venv/bin/activate
flet run --web --port 8550 main_app.py
```

または `python main_app.py`（`FLET_USE_WEB` により挙動が変わります。次節）。

### 4.3 ブラウザで開く

`FLET_USE_WEB=1`（既定）のとき、**Web ブラウザ**で UI が開きます。`run_flet.sh` は既定で **`FLET_SERVER_IP=127.0.0.1`** を渡すため、自動で開くタブは **`http://127.0.0.1:<FLET_PORT>`**（既定ポート 8550）になります。手動で開く場合も同じ URL を使ってください。白紙のタブのままになる場合は、ホストに **`0.0.0.0` を使わない**（ループバック推奨）ことと、ファイアウォールを確認してください。

---

## 5. 表示モード：Web とネイティブ

Flet では **ブラウザ（Web）** と **ネイティブウィンドウ** を切り替えられます。環境変数 **`FLET_USE_WEB`** で統一しています。

| 設定 | 意味 |
|------|------|
| `FLET_USE_WEB=1`（省略時と同じ） | **ブラウザ**で表示。OS のフォルダ選択ダイアログ（`get_directory_path`）は**使えません**（Flet の制限）。 |
| `FLET_USE_WEB=0` または `native` 等 | **ネイティブウィンドウ**。OS のファイル／フォルダダイアログが利用可能な場合があります。 |

**例（ネイティブで起動）：**

```bash
FLET_USE_WEB=0 ./run_flet.sh
```

**例（ポート変更）：**

```bash
FLET_PORT=9000 ./run_flet.sh
```

Web モードでは、フォルダ／画像の指定に **手動パス入力**や、**API が参照できるパス**での**サーバー側フォルダ一覧**（アプリ内のエクスプローラ）が補助として使われます。解析対象データは、**API を動かしているマシン上**（またはマウントされたパス）に置く必要があります。

---

## 6. ログイン

通常起動時は **`/login`** から開始します。入力項目は次のとおりです。

| 項目 | 操作 | 備考 |
|------|------|------|
| Researcher Name | 研究者名を入力 | CSV の Analyst / メタデータの `rater_id` に使われます |
| Password | パスワードを入力 | API の `/login` で検証 |
| Institution code | プルダウン、または **CUSTOM** → 手入力 | 施設を識別する **UPPER_SNAKE** コード（例: `ARIAKE_OHANACHAYA`） |
| Role / 読影担当 | **第1グレーダー**・**第2リーダー**・**最終読影者** | 二重読影・RECHECK再読影の役割。詳細は §10.2〜§10.5 |

### 6.1 施設コードの選び方

1. プルダウンから自施設のコードを選びます。一覧にない場合は **CUSTOM** を選び、研究事務局から指定されたコードを **大文字・アンダースコア**で入力します。  
2. コードは `export/images|masks|meta/{institution_id}/…` や GakuNin RDM 上のフォルダ名と一致させる必要があります。表記ゆれ（半角スペース混入、小文字など）は避けてください。  
3. 環境変数 **`ARIAKE_INSTITUTION_ID`** が端末に設定されている場合、次の両方で **ログイン画面の選択より環境変数が優先**されることがあります。共有端末では食い違いに注意してください。  
   - **Export Metadata** の `export/.../{institution_id}/` パス  
   - 第1グレーダーの **GakuNin RDMへ同期** 先フォルダ名 `{institution_id}/`  
   施設担当者が env とプルダウンを揃えて管理してください。ACL（「どの施設フォルダを一覧で選べるか」）はログイン施設（`login_institution_id`）側です。  
4. **中央読影（研究事務局の横断読影）** 用のコードは **`TEAM_YY`** のみです。プルダウンの **「横浜市立大学」（`YOKOHAMA_CITY_UNIV`）は別の施設プリセット**で、Team YY の横断 ACL にはなりません。中央で全施設を選ぶ作業では必ず **`TEAM_YY`** を選んでください。

### 6.2 Role（読影担当）の選び方

| Role | いつ選ぶか | ログイン後の主な画面 |
|------|------------|----------------------|
| **第1グレーダー（施設読影）** | 通常の施設内解析・第1読影 | 通常のダッシュボード（フォルダ選択 → ROI → 解析） |
| **第2リーダー（セカンドリーディング）** | 二重読影の第2読者として読むとき | ダッシュボード上部に **第2リーダー専用カード**（ローカル自動スキャン／GakuNin 取得）が表示 |
| **最終読影者（RECHECK再読影・トライアッド確定）** | 統合解析データで **NA（RECHECK）** になった主要指標セルを再読影して確定するとき | ダッシュボード上部に **最終読影者カード**（RECHECK対象MD選択）が表示。詳細は §10.5 |

同じ端末で第1グレーダーのあとに第2リーダーが続く場合は、結果画面の **ログアウト** でセッションを破棄してから、Role を切り替えて再ログインしてください（§10.2.5）。ログアウト／再ログイン時に、前回の GakuNin 施設選択（graded / pending）もクリアされます。

### 6.3 ログイン操作（手順）

1. アプリを起動し、ログイン画面を開く。  
2. 研究者名・パスワードを入力する。  
3. Institution code を選ぶ（中央読影なら `TEAM_YY`）。  
4. Role を選ぶ（第1／第2／最終読影者）。  
5. ログインボタンを押す。成功するとホーム（`/`）へ遷移する。  
6. 失敗した場合は画面上のエラー文言を確認し、API（既定 `127.0.0.1:8000`）が起動しているか確認する。

### 開発者向け注意（DEV_MODE）

環境変数 **`DEV_MODE=1`** を付けて起動すると、**ログインがバイパス**され、セッションに開発用ユーザーが入ります。**本番・共有端末では絶対に使わないでください。**

---

## 6A. 多施設・二重読影スキーム（研究運用の全体像）

本アプリの二重読影機能は、**複数施設・複数観察者間で定量手法を揃える**ための運用を支援します。各サイト／Team YY 向けの協働通知（ルート設計・GakuNin フォルダ・ACL）は **[documentation/collaboration.md](documentation/collaboration.md)** です。研究プロトコル上の解析は、おおむね次の二段構えで進めます（詳細な統計計画は別文書）。

1. **各観察時点での MNV（および関連指標）の定量** — 本アプリでの読影・CSV／メタデータ出力。  
2. **治療前後の形態変化と関連因子の探索** — 統合解析データ（採用値）や施設横断データを用いた二次解析。

### 6A.1 読影体制の分岐（どちらで運用するか）

| 施設の状況 | 推奨ルート | 本マニュアルの節 |
|------------|------------|------------------|
| **施設内に独立した読影者が 2 名いる** | 同一端末または同一ローカル出力フォルダで、第1 → ログアウト → 第2 → **統合解析データ** | §10.2（施設内 2 名） |
| **施設内の読影者が 1 名のみ** | 第1読影結果を **GakuNin RDM** へ同期し、**研究事務局の中央読影（アプリでは Institution = `TEAM_YY`）** が第2読影 → RPD 判定 | §10.3〜§10.4 |

いずれのルートでも、指標ごとの一致判定は次のルールです。

\[
\mathrm{RPD}=\frac{|A-B|}{(|A|+|B|)/2}\times 100
\]

- **RPD ≤ 20%**（両値が有限）→ **算術平均を採用**  
- **RPD > 20%** または欠損 → **NA（再計測／合議）** — 統合出力の `*_recheck_list.csv` を参照  

Caliber / Maturity の標準化スコア（**Standardized** = 旧 U2）は解析時点で既定列として出力済みのため、アプリの統合解析データは CSV の値をそのまま突合します（再計算しません）。CLI（`tools/reading_center_rpd/`）のみ採用前に両 CSV で Standardized スコアを再計算します。

> **注意（旧ビルドの CSV）**: アプリの統合解析データは「既定列に標準化スコアが入っている現行パイプラインの CSV」を前提とします。既定列が PCA 値のままの旧ビルド CSV を混ぜると、再計算なしで PCA 値同士を平均します。旧 CSV は CLI（Standardized 再計算あり）で処理してください。

**突合キーの注意（アプリと CLI は別物）**

| 経路 | 突合のしかた | 用途 |
|------|--------------|------|
| アプリ **統合解析データ** | 各行の `File` / `ID` を正規化した **stem** で突合（`dual_grader_merge`） | 施設オペレーター向けの既定フロー |
| CLI `tools/reading_center_rpd/` | 手順書どおり **Case + Visit** キー（オプションで列指定） | リーディングセンター／提出用の再計算 |

同じ CSV ペアでも経路によって採用／NA の集合が変わり得ます。**アプリで出した採用値を「CLI と同じ」とみなさないでください。** CLI 手順は `tools/reading_center_rpd/PROCEDURE_JA.md`。

### 6A.2 データ管理・アクセス制御（アプリ／GakuNin）

- クラウド側は研究で指定された **GakuNin RDM** プロジェクトを用います。Personal Access Token（PAT）はアプリが **OS ネイティブの安全な領域**（Keychain / Credential Manager / libsecret 等）に保存し、ソースコードやコミット対象の `.env` には置きません。  
- **一般施設アカウント**: GakuNin 上では **自施設フォルダの第1読影データのみ**選択・取得できます。他施設データは一覧に出ません。  
- **Team YY（`TEAM_YY`）**: 参加施設の第1読影フォルダを**横断選択**できます。第2結果のアップロード先は施設コードごとに `second_reading/{institution_id}/` へ分離されます。`YOKOHAMA_CITY_UNIV` では横断できません。  
- 第1同期先と第2同期先はパスが異なります（第1の再取得と第2結果が混ざらないようにするため）。**「GakuNin RDMへ同期」は `output_folder` のみ**で、同階層の `integrated_output_*` は対象外です。

```text
GakuNin RDM（プロジェクト配下）/
├── {institution_id}/                    ← 第1グレーダー同期
└── second_reading/
    └── {institution_id}/                ← 第2リーダー結果の同期
```

---


## 7. 画面とナビゲーション

ログイン後、左の**サイドバー**から主要画面へ移動できます。

| アイコン（目安） | 遷移先 | 内容 |
|------------------|--------|------|
| ホーム | `/` | **ダッシュボード**（解析種別・パス指定・バッチ） |
| 解析 | `/` または解析フロー | MNV / VD の各ステップ（`/roi`・`/mnv` など） |
| 結果 | `/results` | 解析結果の表示 |

ルートは URL パスと対応しています（例：`/results`）。

---

## 8. ダッシュボード（ホーム）

### 8.1 解析タイプ

- **MNV Analysis** — 単一画像の MNV 解析（ROI 設定へ進む場合があります）。
- **VD Analysis (Single)** — 単一画像向け VD。
- **VD Analysis (Batch Folder)** — フォルダ単位のバッチ。

**Image Scale (mm)** は画像スケール設定として利用します（解析 API に渡されます）。

### 8.2 画像・フォルダの指定

- **Select Folder / Select Image**  
  - **ネイティブ**：OS のダイアログで選べる場合があります。  
  - **Web**：サーバー側パスを選ぶ UI や、**手動パス**が主な手段です。
- **サーバー側のフォルダ一覧（エクスプローラ型）**でファイルを選んだあと、**「Confirm Selection」** ボタンで確定します。**画像のダブルクリックでは進みません**（仕様）。要点だけ知りたい場合は **[documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md](documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md)** を参照してください。
- **Manual Path**  
  ファイルまたはフォルダの**絶対パス**を貼り付けて利用できます。引用符（`'` `"`）は自動で除かれることがあります。
- **ドラッグ＆ドロップ**  
  対応している場合、ドロップしたパスで処理が始まります。

### 8.3 バッチ解析

フォルダ内の対応画像を一覧に載せ、**Start Batch Analysis** でキュー実行します。完了後、**結果画面**へ進む動作になります。

**対応拡張子の例**（ダッシュボードの走査に基づく）：`.tif` `.tiff` `.png` `.jpg` `.jpeg`

---

## 9. ROI 画面（`/roi`）と MNV ウィザード（`/mnv`）

- **MNV** 系のフローでは、画像と解析設定に応じて **ROI（関心領域）** の指定やウィザード画面が表示されます。
- 従来の **Draw ROI（フリーハンドで取り囲む）** と **Erase Noise（長押しで削る）** に加え、**着色画像**で血管同定（黄＝血管、赤＝拡張）を確認し、**余白trim** で着色のない余白だけを除けます。手順の正本は **[documentation/roi_method.md](documentation/roi_method.md)** です。
- 画面の指示に従い、**戻る／次へ**や**解析開始**を行ってください。
- **Skip — MNV absent**: MNV がはっきり見えない場合に使います。形態計測は実行せず、学習用に **空（全黒）マスク** と **`mnv_present=false`** を残します。CSV では数値は空、`MNV present=0`、`Quality of analysis=N/A`（Fail ではありません。QC と病変有無は別フィールドです）。

---

## 10. 結果画面（`/results`）

- 直近の解析結果やバッチ結果のサマリーを閲覧します。
- **Save CSV** — ImageJ 互換の MNV / VD バッチ CSV を出力フォルダへ保存します。成功後に **ログアウト** や（第2リーダーなら）**統合解析データ** が使えるようになります。
- **Export Metadata & Data** — **MedSAM 型**データ（`export/images|masks|meta/…` + `manifest.csv`）に加え、個別 PDF を **`export/pdfs/{institution_id}/{lesion_id}.pdf`** へ一括書き出します（バックグラウンド実行）。第2リーダー自動スキャンの前提になります。
- **GakuNin RDMへ同期** — セッションの **`output_folder`** を施設スコープのクラウドパスへアップロードします（§10.3・§10.4）。**`integrated_output_*` は含みません。** ローカル保存の代替ではありません。
- データ量が多いと表示に時間がかかる場合があります。

### 10.1 MNV の主要スコア（Complexity / Uniformity）

結果画面のタイルと CSV 列の対応:

| UI | CSV 列 | 意味 |
|----|--------|------|
| **Complexity** | Network Complexity Score | 血管網の複雑性（0–100） |
| **Uniformity** | Caliber Uniformity Score | 口径の均一性（0–100） |
| Maturity Index | Maturity Index | \(50+(\mathrm{Uniformity}-\mathrm{Complexity})/2\) |

**現行実装は層別 PCA**です（マクロ版の「7 成分／6 成分の単純加重」ではありません）。

- **Complexity**: Euler（符号反転）・ループ数・分岐密度・フラクタル次元を Z 化し、層別主成分＋正規化 Trunk を EVR で合成。  
- **Caliber Uniformity**: 放射状 10 bin の径プロファイルから CV・隣接変化・残差 CV・レンジ％を算出し、PCA（0.7·(−PC1)+0.2·PC2+0.1·Trunk）で合成。  
- 層（`small` / `large` / `small_3mm`）は **Image Scale (mm)** と画像幅で決まります。

パラメータ表・式の詳細は **[documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md](documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md) §6.3** を参照してください。Uniformity が厳密に **25.0** のときは再解析を勧める警告が出ます（計算上の固定センチネルではありません）。

### 10.2 二重読影 — 施設内に読影者が 2 名いる場合（ローカル完結）

第1グレーダーの解析結果に対し、同じ施設の第2リーダーが独立して再読影し、**統合解析データ**で RPD ≤ 20% ルールを適用する流れです。**GakuNin を使わず、ローカル（または共有ディスク上の）出力フォルダだけで完結**できます。

#### 10.2.1 第1グレーダー側の準備（必須）

1. Role **第1グレーダー**・自施設コードでログインする。  
2. ダッシュボードで MNV バッチ（または単枚）を実行し、ROI → 解析 → QC を完了する。  
3. 結果画面で次を**この順で**実行する。  
   1. **Save CSV** — `MNV_*.csv` 等を出力フォルダへ保存。成功すると **ログアウト** ボタンが使えるようになります。  
   2. **Export Metadata & Data** — `export/images|masks|meta/{institution_id}/…` と PDF 等を書き出します。第2リーダーの自動スキャンは、このメタデータ／画像ツリーを前提にします。  
4. （任意）同一端末で第2が続く場合は **ログアウト**。別端末の場合は、第1の **出力フォルダ全体**（CSV と `export/` を含む親）を第2が読める場所へコピーまたは共有します。

**前提チェック（第2に渡す前）**

- 出力フォルダに `MNV_*.csv`（または同等のバッチ CSV）がある  
- `export/images/{institution_id}/…` に病変画像がある  
- 施設コードがログイン時と一致している  

#### 10.2.2 第2リーダー — ログインとローカル自動スキャン

1. **ログイン** — 同じ施設コード、Role を **第2リーダー** にしてログインする。  
2. ダッシュボード上部の琥珀色カード **「第2リーダー（二重読影）モード」** を確認する。  
3. **「第2リーダー読影を開始（ローカル自動スキャン）」** を押す。  
4. **第1グレーダーの出力フォルダ**（`export` の親）を選ぶ。  
   - `export` や `meta` そのものを選んでも、アプリが親フォルダへ正規化します。  
   - Web モードでは OS ダイアログが使えないことがあるため、手動パスまたはサーバー側エクスプローラで指定します（§5・§8）。  
5. 成功するとコンソールに画像件数・第1 CSV の有無などが表示され、**エクスポート済み病変画像が順次キュー**に入ります。通常の「ファイルを1枚ずつ選ぶ」操作は不要です。  
6. 出力先は第1のフォルダと**同階層**の `second_reader_output_YYYY_MM_DD/`（施設コード付き名になる場合あり）に自動設定されます。

#### 10.2.3 第2リーダー — 読影・QC

1. キューに入った各画像について、通常どおり次を行う。  
   - ROI 画面: 領域を描画 → **Confirm ROI & Proceed**（再解析時は **Confirm & Re-analyze**）  
   - MNV ウィザード: **Confirm & Start Analysis** 等で解析実行  
   - 結果の QC: **OK — next image** / **Redo ROI** / **Stop Here**  
2. 第1と同じ病変セットを読むことが原則です。意図せず別フォルダをスキャンした場合は、ログアウトしてやり直してください。

#### 10.2.4 第2リーダー — Save CSV と統合解析データ

1. 最終サマリー（またはバッチ完了後の結果画面）で **Save CSV** を押す。  
2. 第2リーダー CSV が `second_reader_output_…/` に保存されると、**「統合解析データ」ボタン**が表示されます（**Save CSV 成功後のみ**）。  
3. **統合解析データ** を押すと、アプリが次を自動実行します。  
   1. 第1グレーダー CSV をスキャン結果から自動検索  
   2. 第2 CSV と **`File` / `ID` の stem** で行を突合（Case+Visit キーの CLI とは異なる — §6A.1）  
   3. 指標ごとに **RPD ≤ 20% → 平均採用**、超過・欠損 → **NA**  
   4. 次を `integrated_output_YYYY_MM_DD/`（第1・第2と同階層）へ出力  

| 成果物 | 内容 |
|--------|------|
| `MNV_integrated_*_adopted_values.csv` | 採用値（提出・二次解析用） |
| `*_avg_fallback.csv` | **参考・暫定**：NA セルを G1/G2 の単純平均で補完した簡易確認用。`is_avg_filled` / `avg_filled_columns` 列で補完セルを識別可。**正式な確定値ではない**（確定値は第3読影者ベースの `triad_median_resolver` 解決結果を使用）。暫定である旨は同名の `*_avg_fallback_README.txt` に明記（Excel でも列がずれないよう CSV 内にはコメント行を置かない） |
| `*_recheck_list.csv` | NA になった主要指標の一覧（再計測対象） |
| `*_summary.md` | RPD 等の要約 |

**フォルダ配置例**

```text
親フォルダ/
├── output_folder_YYYY_MM_DD/              ← 第1（MNV_*.csv, export/…）
├── second_reader_output_YYYY_MM_DD/       ← 第2 CSV
└── integrated_output_YYYY_MM_DD/          ← 採用値・recheck・summary
```

4. `recheck_list` に載った症例は、プロトコルに従い再読影または合議し、必要なら CSV ペアを更新して再統合します。提出用に Case+Visit 突合が必要な場合は CLI（`tools/reading_center_rpd/PROCEDURE_JA.md`）を別途使います（アプリ統合の置換ではありません）。

#### 10.2.5 読影者の交代（ログアウト）

1. **Save CSV**（および必要なら Export / 統合）が終わると、結果画面に **ログアウト** が表示されます。  
2. 押すと、研究者名、Role、第2リーダー用パス、バッチ結果、**GakuNin の graded/pending 施設 ID** などが破棄され、`/login` に戻ります。  
3. 次の読影者が Role・施設コードを選び直してログインします。  
4. **ログアウトせずにブラウザを閉じただけ**だと、端末によってはセッションが残ることがあります。交代時は必ずログアウトするか、再ログイン前にアプリを再起動してください。

---

### 10.3 二重読影 — 施設読影者が 1 名のみ（GakuNin RDM × 中央読影）

自施設で第1読影のみ行い、**研究事務局の中央読影**が第2読影を行うルートです。データ共有には **NII GakuNin RDM** を使います。

**重要:** アプリで横断取得・中央読影を行うときの Institution code は **`TEAM_YY`** です。プルダウンの **`YOKOHAMA_CITY_UNIV`（横浜市立大学）は施設アカウント**であり、Team YY と同じ権限にはなりません。

#### 10.3.1 事前準備（施設・事務局共通）

1. GakuNin RDM 上の **プロジェクト（node）ID** と、必要なら同期ルートの **folder_id** を研究事務局から受け取る。  
2. アプリの **Advanced Settings**（ダッシュボード）で次を保存する（または環境変数 `GRDM_PROJECT_ID` / `GRDM_FOLDER_ID`）。  
   - **GakuNin RDM project_id**  
   - **GakuNin RDM folder_id**（任意。プロジェクト直下以外をルートにする場合）  
3. 初回の同期／取得時に **Personal Access Token（PAT）** を求められます。入力後、接続確認に成功すると **OS の安全な領域に保存**されます（画面のパスワード欄やリポジトリ内 `.env` への平文保存は想定していません）。  
4. `.env.example` を参考にローカル開発用 `.env` を作る場合も、**本物の PAT をコミットしない**でください。

#### 10.3.2 施設側（第1グレーダー）— 解析〜同期

1. 自施設コード・Role **第1グレーダー**でログイン。  
2. 通常どおり MNV 解析を完了する。  
3. 結果画面で **Save CSV** → **Export Metadata & Data** を実行し、ローカルに成果物を残す。  
4. 同じ結果画面の **「GakuNin RDMへ同期」** を押す。  
   - 未設定なら project_id / PAT の入力ダイアログが出ます。  
   - アップロード対象は、結果画面が指す **現在の出力フォルダ（`output_folder`）全体**（再帰）です。第1グレーダーでは通常、CSV と `export/` を含む解析出力フォルダになります。  
   - リモート先フォルダ名は **`{institution_id}/`** です。ここで使う `institution_id` は **`resolve_institution_id`**（**`ARIAKE_INSTITUTION_ID` があればそれが優先**）です。ログイン表示と違うコードに上がることがあるため、共有端末では env を確認してください。  
   - Team YY アカウントでは第1同期は行いません。  
   - 再同期時、同名ファイルはスキップではなく **上書き更新**されます。  
5. 完了メッセージ（件数・施設コード）を確認し、研究事務局へ「同期完了」を連絡する（運用ルールに従う）。  
6. ローカルの CSV / export は施設のバックアップ方針に従って保管する（同期はクラウド複製であり、ローカル保存の代わりではありません）。

#### 10.3.3 中央読影（Team YY）— 取得〜第2読影〜再同期

1. Institution code **`TEAM_YY`**、Role **第2リーダー**でログインする。  
2. ダッシュボードの第2リーダーカードで **「GakuNin RDMから第1読影データを取得して読影開始」** を押す。  
3. 一覧から**対象施設フォルダ**を選ぶ（Team YY は全参加施設が表示されます）。  
4. ダウンロード完了後、自動で第2リーダー用スキャン／キュー投入が走ります。失敗した場合は施設 ID を更新しないため、メッセージに従い再取得またはローカルスキャンでやり直します。  
5. §10.2.3 と同様に ROI → 解析 → QC を実施する。  
6. **Save CSV** → **統合解析データ**（第1 CSV が取得ツリー内にあれば自動突合。無い場合はエラーになるので、第1が CSV ごと同期しているか確認）。採用値はローカルの `integrated_output_…/` に出ます。  
7. 必要に応じて結果画面の **「GakuNin RDMへ同期」** を押す。  
   - 上がるのは **現在の第2リーダー出力フォルダ（`second_reader_output_…` など = セッションの `output_folder`）** です。  
   - **`integrated_output_…`（採用値・recheck・summary）は同階層の別フォルダのため、このボタンではアップロードされません。** 採用 CSV を共有する場合は、別途コピーするか運用で定めた手段を使ってください。  
   - リモート先は **`second_reading/{施設コード}/`**（読影中施設がセッションにあればそれを使用。Team YY で未設定なら施設コード入力ダイアログ）。  
8. 作業終了後は **ログアウト**（別施設の graded ID が次セッションに残らないようにする）。

#### 10.3.4 施設側が自分で第2読影する場合（GakuNin 経由の取得）

施設アカウントでも Role を第2リーダーにし、**「GakuNin RDMから第1読影データを取得して読影開始」** が使えます。ただし一覧は **自施設フォルダのみ**です。他施設データは選択できません。ローカルに第1出力が残っている場合は、§10.2 のローカル自動スキャンの方が速いことがあります。

---

### 10.4 GakuNin RDM 操作の詳細メモ

| 操作 | どこで | 挙動の要点 |
|------|--------|------------|
| project_id / folder_id 設定 | ダッシュボード → Advanced Settings | セッション／client_storage に保持。env でも可 |
| PAT 入力 | 初回の同期または取得時 | OS 安全領域へ保存。接続失敗時は再入力 |
| 同期（アップロード） | 結果画面「GakuNin RDMへ同期」 | **セッションの `output_folder` 全体**を再帰アップロード（第1=解析出力、第2=`second_reader_output_*`）。**`integrated_output_*` は含まない**。リモートは第1=`{inst}/`、第2=`second_reading/{inst}/` |
| 取得（ダウンロード） | 第2リーダーカード | ACL 適用後に施設フォルダを選択 → 隔離されたローカル配下へ展開 → 読影キュー |
| 上書き | 再同期 | 既存ファイルは更新（永久スキップしない） |
| ログアウト | 結果画面 | graded/pending 施設キーもクリア |

**うまくいかないとき（追加）**

| 現象 | 確認 |
|------|------|
| 「自施設の第1読影データがありません」 | 第1が同期済みか、ログイン施設コードとリモートフォルダ名が一致しているか（第1同期は env `ARIAKE_INSTITUTION_ID` 優先にも注意） |
| Team YY なのに施設が一覧に出ない | Institution が本当に **`TEAM_YY`** か（`YOKOHAMA_CITY_UNIV` ではないか）。リモート直下が施設コード名か |
| 統合解析データで第1 CSV が見つからない | 第1が Save CSV したファイルが同期／スキャン対象に含まれているか |
| 採用 CSV が GakuNin に無い | **仕様どおり**。同期は `output_folder` のみ。`integrated_output_*` は手動共有 |
| PAT を保存できない | OS のキーリング／資格情報ストアが利用可能か。平文バックエンドは拒否されます |
| 別施設のパスに書き出された | 前回の Team YY セッションが残っていないか → ログアウトして再ログイン |

---

### 10.5 最終読影者 — RECHECK再解析とトライアッド中央値確定

統合解析データ（§10.2.4）で **NA（RECHECK）** になった主要指標セルを、最終読影者が追加読影し、**G1・G2・最終読影者の3値の中央値（median）を確定値として採用**するワークフローです。3値のうち1値が外れても中央値は動かないため頑健です。

#### 10.5.1 前提

- 統合解析データの出力（`integrated_output_…/` 内の `*_summary.md`・`*_recheck_list.csv`・`*_adopted_values.csv`）が揃っていること。summary MD の **RECHECK セクションに「症例別（NA となった主要指標）」リスト**が含まれます。
- 第1グレーダーの `export/images|masks|meta/…` バンドルが同じ親フォルダ配下に残っていること（対象症例画像の探索に使用）。

#### 10.5.2 手順

1. **ログイン** — Role を **最終読影者** にしてログインする。  
2. ダッシュボード上部の紫色カード **「最終読影者モード」** から **「RECHECK対象MDファイルを選択して再読影開始」** を押し、ファイル選択で対象の `*_summary.md` を**毎回手動で**選ぶ（フォルダを確定した場合は最新の summary MD を自動選択）。  
3. MD がパースされ、**RECHECK対象の症例画像のみ**が通常の ROI → 解析キューに入る。  
   - **未知のパラメータ表記がある場合は処理を中止してエラー表示**します（サイレント無視しません）。MD の表記を確認してください。  
4. 通常どおり ROI → 解析 → QC を実施し、結果画面で **Save CSV** を押す（出力は同階層の `final_reader_output_…/`）。  
5. **「トライアッド確定」** ボタンを押す。まず **dry-run プレビュー**（全セルの G1／G2／最終読影者／中央値／CV%／要レビューの一覧。**この時点では何も書き込まれません**）が表示されます。  
6. 内容を確認し **「本確定を実行」** を押すと、`integrated_output_…/` に以下が**新規ファイル**として出力されます。

| 成果物 | 内容 |
|--------|------|
| `*_triad_resolved_cells.csv` | セル別監査レコード（`g1_value` / `g2_value` / `final_reader_value` / `final_value` / `needs_review` / `cv_percent` 等） |
| `*_triad_adopted_values.csv` | adopted CSV に RECHECK 中央値・再読影像の残数値NA中央値・Subtype/Pathophysiology（Vsl Area中央値グレーダー）を適用 + `Triad Needs Review (metrics)` 列 |
| `*_triad_avg_fallback.csv` | **参考・暫定**：トライアッド後も残った NA を既存の G1/G2 単純平均（`*_avg_fallback.csv`）で埋めたもの。`is_avg_filled` で識別。**正本ではない**（同名 `*_triad_avg_fallback_README.txt`） |
| `*_triad_summary.md` | ルール・集計・セル別テーブル |

#### 10.5.3 確定ルール

- `final_value = median(G1, G2, 最終読影者)`（G1/G2 の一方が欠損なら2値の中央値=平均）。  
- **最終読影者が再読影した画像**に残っている数値 NA（主要指標以外で RPD>20% だったセル）も、同じ三人中央値で埋めます。**すでに採用済みの値（RPD≤20% の平均）は上書きしません。** 最終読影 CSV に無い画像（RECHECK 対象外）は触りません。  
- **Subtype / Pathophysiology が NA** の再読影像は、**Vsl Area (mm2) のトライアッド中央値を出したグレーダー**の分類を採用します（3値が異なるときは中央値そのものを出した1人。同値なら最終読影者を優先）。  
- **RPD(median, 最終読影者値) > 20%**（既存のNA/採用判定と同一閾値）の場合、`needs_review = true` フラグが付きます。**値は確定され、処理はブロックされません**（プロトコルに従い事後レビュー）。  
- 3値の **CV%**（SD/平均×100）を保持します（トライアッドの再現性はペアワイズRPDではなく CV%/ICC で報告する方針）。  
- 元の `*_adopted_values.csv` / `*_recheck_list.csv` は変更されません。  
- 参考出力 `*_triad_avg_fallback.csv` は、トライアッド後も残った NA を既存の G1/G2 平均で埋えた**暫定ファイル**です。新しい解析はしません。提出には `*_triad_adopted_values.csv` を使ってください。

詳細仕様は [documentation/triad_recheck/README.md](documentation/triad_recheck/README.md) を参照してください。

---

## 11. トラブルシューティング

| 現象 | 確認すること |
|------|----------------|
| ブラウザが開かない | `FLET_PORT` が他プロセスと競合していないか、ファイアウォールを確認。 |
| API に接続できない | バックエンドが起動しているか、`127.0.0.1:8000` に届くか。 |
| Web でフォルダが選べない | **仕様上、OS のフォルダダイアログは使えません。** 手動パス、サーバー側エクスプローラ、または **`FLET_USE_WEB=0`** でネイティブ起動を試す。 |
| パスが通らない | パスが **API プロセスから見える**か（共有フォルダのマウント、権限）。 |
| ログインできない | `DEV_MODE` ではないことを確認。API の認証とネットワークを確認。 |
| 第2リーダーカードが出ない | Role が **第2リーダー** か。ログアウトして Role を選び直す。 |
| 統合解析データボタンが無い | 先に **Save CSV** で第2 CSV を保存したか。 |
| 最終読影者カードが出ない | Role が **最終読影者** か。ログアウトして Role を選び直す。 |
| MD選択で「未知のパラメータ」エラー | MD の指標表記がマッピング表（§10.5.2／`documentation/triad_recheck/README.md`）にあるか。手編集による typo を確認。 |
| トライアッド確定ボタンが無い | MD選択から再読影を開始したか、先に **Save CSV** で最終読影者 CSV を保存したか。 |
| GakuNin 同期／取得に失敗 | project_id・folder_id・PAT・ネットワーク。§10.4 の表も参照。 |
| 他施設データが見える／見えない | 一般施設は自施設のみが正常。横断が必要なら **TEAM_YY** でログイン。 |

---

## 12. ファイルとログ

| ファイル / 場所 | 用途 |
|-----------------|------|
| `run_flet.sh` | バックエンド + Flet の一括起動 |
| `main_app.py` | Flet エントリ（`FLET_USE_WEB` / `FLET_PORT` / `DEV_MODE`） |
| `src/api/main.py` | FastAPI バックエンド |
| `uploads/` | アップロード用（実行時に作成されることがある） |
| `backend.log` | API 起動をスクリプトでリダイレクトしている場合のログ例 |
| `export/images|masks|meta/{institution_id}/` + `manifest.csv` | Metadata & Data（MedSAM 型・OCTA MNV ROI） |
| `.env.example` | `GRDM_PROJECT_ID` / `GRDM_FOLDER_ID` のひな型（PAT は OS 安全領域推奨） |
| `tools/reading_center_rpd/` | 二重 CSV の RPD20 採用（CLI・手順書） |

---

## 13. 注意事項

- 本アプリの利用は、施設の**情報セキュリティ方針**と**臨床研究倫理**に従ってください。
- 解析の医学的解釈は、**専門家の判断**を前提とし、本マニュアルはソフトの操作手順の説明に限ります。
- GakuNin RDM の PAT や施設データを、チャット・メール・リポジトリに貼り付けないでください。
- 統合解析データの **NA（再計測）** を、無断で平均値に置き換えないでください。自動出力される `*_avg_fallback.csv` およびトライアッド後の `*_triad_avg_fallback.csv` は参考・簡易確認用の**暫定補完**であり、提出・二次解析には `*_adopted_values.csv` / `*_triad_adopted_values.csv` を使用してください。

---

## 14. 関連ドキュメント

- **操作の要点（Confirm Selection 等）:** [documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md](documentation/ARIAKE_OCTA_操作マニュアル_簡易版.md)
- **多施設グレーディング通知（各サイト／Team YY）:** [documentation/collaboration.md](documentation/collaboration.md)
- **ROI の指定方法（手動囲み・Erase・着色画像／余白trim）:** [documentation/roi_method.md](documentation/roi_method.md)
- **理論・出力定義・Complexity / Caliber Uniformity:** [documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md](documentation/ARIAKE_OCTA_詳細ユーザーマニュアル_V2.md)（§6.3）
- **RPD20 採用（CLI 手順）:** [tools/reading_center_rpd/PROCEDURE_JA.md](tools/reading_center_rpd/PROCEDURE_JA.md)
- **開発者向け（実装・拡張）:** [DEVELOPER.md](DEVELOPER.md)

---

## 15. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-21 | トライアッド確定時に **`*_triad_avg_fallback.csv`**（残NAを既存 G1/G2 平均で埋めた参考・暫定）を追加出力。§10.5.2・§10.5.3 を更新。 |
| 2026-08-21 | 最終読影者が再読影した画像の**残数値 NA**をトライアッド中央値で補完。Subtype/Pathophysiology の NA は **Vsl Area 中央値のグレーダー**の分類を採用。§10.5.3 を更新。 |
| 2026-08-21 | 各サイト／Team YY 向け協働通知 [collaboration.md](documentation/collaboration.md)、ROI 手順 [roi_method.md](documentation/roi_method.md) を公開文書として追加。§9 に着色画像／余白trim への案内を追記。 |
| 2026-08-15 | **最終読影者（RECHECK再読影・トライアッド確定）機能**を追加：ログイン Role に最終読影者、RECHECK対象MDの選択→対象症例のみ再読影、median(G1, G2, 最終読影者) による確定、要レビューRPD 20%（既存閾値流用）、CV% 保持、dry-run プレビュー→本確定の2段階。§6・§10.5・トラブルシュートを追記。 |
| 2026-08-15 | 統合解析データに **`*_avg_fallback.csv`**（NA セルを G1/G2 単純平均で補完した**参考・暫定**ファイル。`is_avg_filled` 列と同名 `*_avg_fallback_README.txt` で暫定である旨を明示）を追加出力。§10.2.4・§13 を更新。 |
| 2026-08-12 | Bugbot 指摘反映: アプリ統合と CLI の突合キー差、同期対象=`output_folder`（integrated 非対象）、`ARIAKE_INSTITUTION_ID` の同期への影響、`TEAM_YY`≠`YOKOHAMA_CITY_UNIV`、ROI ボタン正式名。 |
| 2026-08-12 | **多施設二重読影＋GakuNin RDM** を操作マニュアルに詳細追記。§6（ログイン細分）、§6A（スキーム全体像・RPD20・ACL）、§10.2（施設内2名の手順）、§10.3（1名＋中央読影の同期／取得）、§10.4（GRDM 操作メモ）、トラブルシュート／関連ドキュメントを更新。 |
| 2026-08-08 | 第2リーダー出力を第1グレーダーの出力フォルダと**同階層**（`second_reader_output_YYYY_MM_DD/`）に、統合解析データを同階層の **`integrated_output_YYYY_MM_DD/`** に変更。エキスポート後に **ログアウト** ボタンを追加（同一端末での第1グレーダー→第2リーダー交代動線）。§10.2 を更新。 |
| 2026-08-07 | **第2リーダー（二重読影）機能**を追加：ログインの Role 選択、メタデータ親フォルダの自動スキャン、エキスポート後の **統合解析データ** ボタン（RPD≤20% 採用ルールで第1・第2 CSV を統合）。§6・§10.2 を追記。 |
| 2026-08-07 | ログインに **Institution code** を追記。結果画面に **Export Metadata & Data**（`export/{institution_id}/{lesion_id}/`）を追記。§10.1 に **Complexity / Caliber Uniformity**（層別 PCA）の要約と詳細マニュアル §6.3 へのリンクを追加。関連ドキュメント・ログ表を更新。 |
| （既往） | 起動（`run_flet.sh`）、Web／ネイティブ切替、ログイン、ダッシュボード、ROI／結果画面、トラブルシューティングを記載。 |

---

*文書の対象：リポジトリ内の Flet フロントおよび付属起動手順。バックエンド API の詳細は `src/api` および利用環境の仕様に従ってください。*
