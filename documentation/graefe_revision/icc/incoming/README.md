# Incoming multi-rater ICC data — **COMPLETED**

Observer score CSVs for the n=46 Comment 4 cohort are already in place. This folder is an **archive of received drops**, not an open collection task.

```
incoming/
  observer_YY/          ← Session 1 (inter primary)
  observer_A/           ← alias folder (same role as Inoue if used)
  observer_B/           ← alias folder (same role as Osada if used)
  observer_inoue/
  observer_osada/
```

**Canonical protocol, ICC numbers, and recompute commands:** [`../README.md`](../README.md).

Do **not** mix legacy Flet `icc_session1.csv` / `icc_session2.csv` (different image set) with these batches.

Pre-completion drop instructions (historical): [`../../_archive/icc_incoming_README_pre_completion.md`](../../_archive/icc_incoming_README_pre_completion.md).
