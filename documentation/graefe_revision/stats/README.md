# stats/ — Table 3 effect sizes — **COMPLETED**

Outputs from `scripts/graefe_revision/table3_effect_sizes.py` (Kruskal–Wallis + ε² + bootstrap CI).

| File | Role |
|------|------|
| `table3_effect_sizes.md` | Summary table |
| `table3_effect_sizes.csv` | Same as CSV |
| `table3_per_case_scores.csv` | Per-case scores used |

**Manuscript / Response narrative:** Comment 5 uses the **default** device-locked Standardized Caliber Uniformity Score (between-stratum contrasts NS / ε²≈0). Legacy PCA Caliber/Maturity ε²≈0.24 is sensitivity only — see Response, not this folder’s older PCA-primary framing if any numbers differ.

Parent map: [`../README.md`](../README.md).
