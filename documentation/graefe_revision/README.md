# Graefe major revision (local)

**Do not push this branch to remote.**

- Branch: `graefe/major-revision-analyses` — **local only**
- Working tree: `documentation/graefe_revision/` and related scripts

Never run `git push` for this revision work unless the user explicitly asks.

See `REVISION_CHECKLIST.md` for the full work plan and status.
