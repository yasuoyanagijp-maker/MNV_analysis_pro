# =============================================================================
# ARIAKE_MNV requirements.txt
# Generated: 2026-02-04
# Tested with: Python 3.9+ (recommended: 3.10-3.12)
# =============================================================================
# CRITICAL: Do not upgrade these versions without testing!
# streamlit-drawable-canvas requires streamlit<1.29.0 due to image_to_url API
# numpy<2.0 is required for compatibility with scikit-image and opencv
# pandas>=2.1.x needed for Python 3.12 wheels (2.0.x has no cp312 wheel)
# =============================================================================

# ------------------
# Core Scientific Stack
# ------------------
numpy==1.26.4
scipy==1.13.1
pandas==2.1.4

# ------------------
# Image Processing
# ------------------
scikit-image==0.24.0
opencv-python==4.9.0.80
Pillow==10.4.0
tifffile==2024.8.30
imageio==2.37.2
imagecodecs>=2023.1.0  # LZW/compressed TIFF support for tifffile

# ------------------
# Streamlit & Flet UI
# ------------------
streamlit==1.28.0
streamlit-drawable-canvas==0.9.3
# NOTE: pin blinker for stability with Streamlit 1.28 runtime event loop
blinker==1.8.2

# New UI
flet==0.28.3
# Native desktop window (FletApp). Must match flet; PyInstaller EXE needs this bundled — else runtime pip fails.
flet-desktop==0.28.3
# Flet optional dep: required for flet.security (PyInstaller collects submodules; avoids hook WARNING)
cryptography>=42.0.0

# ------------------
# API & Async
# ------------------
fastapi==0.110.0
uvicorn==0.29.0
httpx==0.27.0
python-multipart==0.0.9  # Required for file uploads in FastAPI

# ------------------
# Graph & Spatial Analysis
# ------------------
networkx==3.2.1
sknw==0.15
shapely==2.0.7

# ------------------
# Visualization
# ------------------
# matplotlib: ROI debug (optional) + batch chart PDF (src/utils/mnv_results_chart.py)
matplotlib==3.9.4

# ------------------
# System & Utilities
# ------------------
psutil==7.2.2
threadpoolctl>=3.1.0
requests>=2.31.0
python-dotenv>=1.0.0
# OS-native secure store for GakuNin RDM PAT (Keychain / Credential Manager / libsecret).
# flet-secure-storage requires flet>=0.80 and conflicts with flet==0.28.3; when upgrading
# Flet, prefer flet-secure-storage — src/utils/grdm_secure_storage.py already tries it first.
keyring>=24.0.0

# PDF export (src/utils/report_generator.py, src/utils/mnv_results_chart.py; PyPI: fpdf2, import: fpdf)
fpdf2==2.8.4
defusedxml==0.7.1
fonttools==4.60.2

# ------------------
# Development & Testing
# ------------------
pytest>=7.0.0
black>=22.0.0
isort>=5.10.0
flake8>=5.0.0

# ------------------
# Optional: Numba acceleration (if needed)
# ------------------
# numba==0.60.0
# llvmlite==0.43.0

# ------------------
# Optional: macOS/Windows packaging (see build_mac.sh / build_win.sh)
# Install separately: pip install -r requirements-build.txt
# Versions intentionally unpinned to avoid conflicts with runtime pins above.
# ------------------
